# native Wayland everywhere - avoids spawning Xwayland (~150 MB)
MOZ_ENABLE_WAYLAND=1
GDK_BACKEND=wayland,x11
QT_QPA_PLATFORM=wayland;xcb
SDL_VIDEODRIVER=wayland
# no accessibility bus unless explicitly needed (~15 MB)
NO_AT_BRIDGE=1
